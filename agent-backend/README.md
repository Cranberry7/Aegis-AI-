# Agent-Backend

node required version => 22.13

## First Time Setup - 
* ```npm i``` to install all ```node_modules```
* ```mkdir config touch .env``` make config dir and add .env file
* ```npm run start:dev``` to start server
